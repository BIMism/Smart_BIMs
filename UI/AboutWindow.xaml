<Window x:Class="Smart_BIMs.UI.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Smart BIMs" Height="350" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#000000"
        WindowStyle="ToolWindow">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <!-- Logo Area (User will replace logo.png) -->
            <Image Source="pack://application:,,,/Smart_BIMs;component/Resources/logo.png" Height="80" Margin="0,0,0,20"/>
            
            <TextBlock Text="Smart BIMs" Foreground="#FFFFFF" FontWeight="Light" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="v 1.0.0" Foreground="#007BFF" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            
            <Button Content="WWW.ACADEMYINNOV.COM" Width="260" Height="40" Click="WebLink_Click"
                    Background="Transparent" Foreground="#FFFFFF" BorderBrush="#FFFFFF" BorderThickness="1" FontSize="14" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#007BFF"/>
                                            <Setter Property="BorderBrush" Value="#007BFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
